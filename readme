## 一、技术栈选型（全是代码，无需环境）

方向二是一个**纯代码实现**的方案，所有工作都可以在本地 Python 环境中完成。技术栈选型如下：

| 类别 | 推荐工具/库 | 用途说明 |
|------|------------|---------|
| **编程语言** | Python 3.8+ | 主开发语言 |
| **数据处理** | Pandas、NumPy | CSV文件读取、数据清洗、特征标准化、缺失值处理等全流程预处理 |
| **数据可视化** | Matplotlib、Seaborn | 实验图表绘制（数据分布、混淆矩阵热力图、性能对比柱状图等） |
| **机器学习** | scikit-learn | 特征编码、数据集划分、决策树/随机森林/MLP神经网络模型训练、准确率/精确率/召回率/F1值评估 |
| **深度学习（可选）** | TensorFlow / PyTorch | 如需搭配深度神经网络做论文对比分析时使用（非必须，但可提升论文创新性） |

这个python项目
你直接which conda找到我配置好的miniconda环境就好，里面都配置好的
另外我们可以做一点深度学习的部分
数据集也下好了zip，你解压就是
然后因为这是一个分工任务，但是是我一次做完，在设计milestone的时候需要你明显分成三个人的工作，这样我好分配成果
额外说明就是这样，可以开始初始化项目了
**最小安装命令：**
```bash
pip install pandas numpy matplotlib seaborn scikit-learn
```

**（可选）如需深度神经网络对比：**
```bash
pip install tensorflow  # 或 torch
```

---

## 二、NSL-KDD数据集下载与获取

### 2.1 数据集概述

NSL-KDD是对KDD99数据集的改进版本，包含125,973条训练记录和22,544条测试记录，共使用**41个特征**来描述每条网络流量。每条流量都带有标签（normal或anomaly），是一个**二分类异常检测数据集**。攻击类型分为四大类：DoS（拒绝服务）、Probe（探查扫描）、R2L（远程到本地攻击）、U2R（用户到根攻击）。

### 2.2 官方/可靠下载渠道

| 渠道 | 链接 | 说明 |
|------|------|------|
| **国家基础学科公共科学数据中心** | https://www.nbsdc.cn/general/dataDetail?id=67d50d83195d260905af969e&type=1 | CSTR标识：16666.11.nbsdc.dCYS2iBf，数据量约681.78MB，中国官方学术平台 |
| **Kaggle（推荐首选）** | https://www.kaggle.com/datasets/hassan06/nslkdd | 最常用的下载入口，包含KDDTrain+.txt和KDDTest+.txt等完整文件 |
| **Kaggle备用（预过滤版）** | https://www.kaggle.com/datasets/harmannahal/nsl-kdd-dataset | 已将分类特征预过滤为前10个常用值，可简化one-hot编码 |
| **Zenodo（V2扩展版）** | https://zenodo.org/records/10141556 | 数据已归一化，数据量72.7MB，包含scaler.pickle |
| **原UNB网站（已不可用）** | https://www.unb.ca/cic/datasets/nsl.html | 官方页面已显示“数据集不再可用”，但保留了完整的数据结构文档 |

### 2.3 数据文件说明

下载后主要使用以下两个文件：

| 文件名 | 用途 | 记录数 | 格式 |
|--------|------|--------|------|
| `KDDTrain+.TXT` | 训练集 | 125,973条 | CSV格式，含41个特征+攻击类型标签+难度等级 |
| `KDDTest+.TXT` | 测试集 | 22,544条 | CSV格式，含41个特征+攻击类型标签+难度等级 |

数据集中的攻击类型包含：`normal`、`neptune`、`warezclient`、`ipsweep`、`portsweep`、`teardrop`、`nmap`、`satan`、`smurf`、`pod`、`back`、`guess_passwd`、`ftp_write`、`multihop`、`rootkit`、`buffer_overflow`、`imap`、`warezmaster`、`phf`、`land`、`loadmodule`、`spy`、`perl` 共23种标签。需要注意的是，NSL-KDD是**二分类异常检测数据集**（标签分为normal和anomaly），训练集虽然包含23种攻击类型名称，但将其映射为anomaly类别即可完成二分类任务，论文中既可以做二分类（正常/异常）也可以做多分类（具体攻击类型识别）。

---

## 三、完整实验流程（关键环节）

建议按以下步骤组织实验代码和论文结构：

| 阶段 | 核心任务 | 推荐工具/方法 |
|------|---------|--------------|
| **数据集加载与探索** | 读取CSV、定义41个特征名称、观察数据分布 | Pandas |
| **数据清洗** | 处理缺失值（NSL-KDD缺失值极少）、删除不相关列（如难度等级`level`） | Pandas |
| **类别特征编码** | `protocol_type`、`service`、`flag`转为数值；注意有3个分类特征，各含约60–70个唯一值，需要进行编码处理 | `LabelEncoder` / `OneHotEncoder` |
| **特征标准化** | 数值特征归一化/标准化 | `StandardScaler` / `MinMaxScaler` |
| **数据集划分** | 按7:3或8:2划分训练/验证（或直接使用官方分割） | `train_test_split` |
| **特征选择（可选）** | 使用RFE、随机森林特征重要性筛选 | `RFE` / `SelectKBest` |
| **多模型训练** | 决策树、随机森林、神经网络（MLP）三模型进行横向对比 | `DecisionTreeClassifier` / `RandomForestClassifier` / `MLPClassifier` |
| **模型评估** | 准确率、精确率、召回率、F1值、混淆矩阵、AUC-ROC | `classification_report` / `confusion_matrix` / `roc_auc_score` |
| **对比分析** | 表格 + 柱状图对比三大模型在不同攻击类型上的表现 | Pandas + Matplotlib / Seaborn |
| **深度学习方法（可选扩展）** | CNN、LSTM用于实验扩展和论文创新性提升 | TensorFlow / PyTorch |

---

## 四、参考学术资料

撰写论文时可以参考以下代表性工作：

1. **NSL-KDD官方论文**：M. Tavallaee, E. Bagheri, W. Lu, A. Ghorbani, “A Detailed Analysis of the KDD CUP 99 Data Set,” *IEEE CISDA*, 2009——NSL-KDD数据集的官方定义论文，论文开篇必引
2. **深度学习IDS综述（2025年）** ：《AI-Enhanced Intrusion Detection System Using Deep Learning on NSL-KDD Dataset》，2025年11月发布，涵盖CNN、RNN、LSTM等架构
3. **随机森林与半监督学习** ：《Network Intrusion Detection Method Based on Semi-Supervised Learning and Random Forest》，2025年10月，NSL-KDD上准确率提升8.92%
4. **特征筛选优化** ：《A hybrid CNN-LSTM approach for intelligent cyber intrusion detection system》，结合RFE与决策树进行特征选择
5. **中文入门资料**：《NSL-KDD数据集介绍（附下载链接）》，CSDN博客，包含41个特征的完整含义表格

---

## 五、多人分工建议

| 角色 | 负责任务 |
|------|---------|
| **A同学（数据方向）** | 数据加载与探索分析、缺失值处理、特征标准化、数据集划分、论文数据部分撰写 |
| **B同学（模型方向）** | 决策树与随机森林模型搭建、模型调优、参数网格搜索 |
| **C同学（对比分析）** | MLP神经网络搭建、模型评估、可视化图表绘制、性能对比分析、论文图表制作 |

三人都各自负责对应模型（决策树/随机森林/MLP）的**参数调优**，最后合并分析结果，确保横向对比的完整性。

---

## 六、论文撰写建议

**可以重点补充的创新点：**
- 特征选择方法对比（如基于方差阈值、卡方检验、随机森林特征重要度）
- 针对NSL-KDD中样本不平衡问题（U2R/R2L攻击样本极少），探讨过采样（SMOTE）或欠采样技术
- 将实验部署到局域网ARP攻击环境下讨论适配性（任务书中明确要求）

---

## 七、重要注意事项

1. **计算机视觉库（OpenCV）不需要安装**，NSL-KDD是网络流量表格数据，全部任务都可以用Pandas/NumPy + scikit-learn + Matplotlib完成
2. **CSIC 2010 / SIMARGL2021 等数据集与本方向无关**，方向二只用NSL-KDD
3. **UNB官网已不可下载**，优先使用Kaggle或国家基础科学数据中心
4. **文献检索建议**：在知网（https://www.cnki.net）搜索“NSL-KDD 入侵检测 机器学习”，可获取大量高质量中文论文用于论文综述